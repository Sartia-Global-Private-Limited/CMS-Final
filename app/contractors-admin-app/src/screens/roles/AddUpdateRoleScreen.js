import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const AddUpdateRoleScreen = () => {
  return (
    <View>
      <Text>AddUpdateRoleScreen</Text>
    </View>
  );
};

export default AddUpdateRoleScreen;

const styles = StyleSheet.create({});
